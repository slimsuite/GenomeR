#!/usr/bin/env Rscript

dependencies <- c("shiny", "shinyjs", "shinyWidgets", "shinycssloaders", "plotly", "quantmod", "tidyverse")
install.packages(dependencies, repos = "https://cloud.r-project.org/")